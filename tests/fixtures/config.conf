#!/usr/bin/env bash
# Test-only overrides for the security-assessment tooling.
# Merged on top of config/config.conf via SAT_CONFIG_OVERRIDE.

AUTO_EXPLOIT=false
REQUIRE_EXPLOIT_APPROVAL=true
SEARCHSPLOIT_ENABLED=true
METASPLOIT_ENABLED=true
METASPLOIT_TIMEOUT=30